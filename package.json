{
  "name": "maytapi",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.7.0"
  },
  "scripts": {
    "generate": "npx @openapitools/openapi-generator-cli generate -i ./maytapi.json -g typescript --additional-properties=platform=deno -o ./"
  }
}
